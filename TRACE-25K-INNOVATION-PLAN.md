# 25K 合成数据固定调用协议与提效方案

## 1. 不可变目标

主口径按用户最新定义：发出 25,000 次采样模型 API 请求，每次产生一条候选合成数据；在
每条成功候选固定接受两次 Luna 评分的前提下，至少 20,000 条进入 KTO 有效区间，即
`Accept / sampling requests >= 80%`。若“25K”实际指 25,000 条已接受数据，则在恰好 80%
通过率下至少需要 31,250 次采样请求，这作为备选预算单独报告，不能与主口径混用。

对每个根样本，在线事务严格固定为：

```text
本地确定性上下文编译（0 次模型调用）
  → 1 次 Qwen API 请求，生成 1 条最终回答
  → Luna Ground Judge 1 次
  → Luna Structure Judge 1 次
  → 入库或拒绝
```

硬约束：

- 当前冻结采样模型为 `opencode-go/qwen3.8-max`；未来模型池必须在生产前写入 manifest 并冻结；
- 评分模型固定为 `gpt-5.6-luna`，reasoning effort 为 `low`；
- Judge prompt 与评分规则不得修改；
- Accept 固定定义为 `score >= 14` 或 `0 < score <= 8`，`score == 0` 不可用，`9..13` 为模糊区；
- 不允许 Luna 预规划、Luna 修订、第三次 Luna 调用或任何其他教师调用；
- 不允许失败后再调用 Qwen 重写、补写、Best-of-N 或重试；
- 每个网络操作 `max_attempts = 1`；中断、超时和 HTTP 失败均直接结束该根样本；
- 本地规则不能批准未经过两次冻结 Judge 的候选。

## 2. 唯一生产效率口径

```text
效率 = 落入 KTO 有效正负区间的 Qwen 最终回答数 / 实际发出的 Qwen API 请求总数
```

分母包括成功、HTTP 失败、超时、中断以及任何意外重试。虽然实现会禁止自动重试，审计仍以
底层请求事件数为准，不能用“根样本数”或“成功响应数”替代。

目标是观察效率大于或等于 80%。其中正样本为 `score >= 14`，负样本为
`0 < score <= 8`。Qwen 成功但任一 Judge 未完成的样本不能进入
分子；其 Qwen 请求仍保留在分母。

同时报告但不混淆：

- Qwen 传输成功率；
- 两次 Luna 评分完整率；
- `accepted / Qwen requests` 主指标；
- 失败规则分布；
- exact/near dedup 后的最终有效数据量。

## 3. 当前证据边界

截至 2026-08-12：

- 在调用计数上合规的早期 10 根开发臂为 8/10，即达到当前 80% 点估计门槛；该臂
  在一次 Qwen 响应内返回两个草稿再由本地选择，故只作为算法探索证据，不沿用其输出形态；
- 合并更多相同策略的开发证据后，严格按中断请求计入分母为 11/16，即 68.75%；
- 被拒样本主要是事实、材料一致性和可视化表达问题，而不是单纯 Markdown 格式问题；
- 曾尝试的 Luna 预规划 v6 多用一次 Luna，协议违规，已停止、从 CLI 移除并永久排除于指标。
- v7 Context Compiler 在 8 次 Qwen 请求后为 6/8（75%）；协议为 8Q/16L，但其历史排除并集
  后经审计发现不完整，因此不能称为全新根验证；
- v8 的强制模板与本地删除守卫在真正新根上 0/2 后止损（2Q/4L），证明过度约束会伤害
  语义一致性，已放弃该方向；
- v9 静默三陪审团在全历史排除后的新开发根上达到 9/10（90%，10Q/20L），随后冻结提示；
- 冻结 v9 在 exact-root-disjoint validation split 上于 20/30（66.67%）触发数学止损；实际为
  30Q/58L，其中 1 次 Qwen 失败、2 次 Luna 失败。即使未运行的 20 根全部通过，完整 50 根
  也最多为 40/50（80%）；该止损发生在目标尚定义为严格大于 80% 的历史阶段；
- validation 内容侧聚合失败以 `FACT_LOGIC_ISSUE=5`、`LACK_VISUAL_AID=4`、
  `CONTRADICT_KB_OR_EXPERT=3` 为主。剩余 validation 与 audit roots 保持封存，不用于调参。
- v10 查询相关性 Evidence Packet 在 2 个新开发根上 0/2（2Q/4L）后止损；仅保留 top-k
  片段造成材料过度截断，出现回答不完整、核心答案埋藏和内容偏薄，因此该方向已放弃；
- v11 改为“完整原始上下文 + 只读相关性索引”。在三组开发/审计运行中，已完成双评审的
  候选合计 12/13 通过，但共享 Luna 的 502 使用户口径明显下降，说明此前同时存在内容与
  基础设施两个变量，不能把 12/13 当作生产效率；
- 为隔离基础设施变量，部署了三个独立、单并发 Luna 端点并以 90 秒最小启动间隔轮转；冻结
  v11 的 exact-root-disjoint audit 最终在 6 根时止损，严格账本为 6Q/12L，18 次调用全部成功、
  无重试，Accept 为 4/6（66.67%）。两个未通过分别含致命 `NUM_COMPARE_ERROR` 和
  `PERSONAL_DATA_MISMATCH`，证明稳定端点下的剩余瓶颈仍是内容忠实度；
- 该 audit 的单侧精确 95% 下界为 27.13%，点估计和统计下界均未达到目标。因为第 2 个
  未通过已经使预注册 10 根的理论上限成为 8/10，运行器按规则停止，没有继续消耗额度。

小样本开发结果只用于选择 prompt，不能宣称已经实现稳定的生产通过率。

## 4. 合规创新：确定性 Context Compiler

不新增模型调用，在 Qwen 请求前由本地代码把原始输入编译成显式证据包。证据包只重排和标记
原文，不生成新事实：

1. **任务路由**：识别回答属于趋势比较、单次状态、计划建议、课程推荐或知识解释；
2. **事实账本**：抽取并保留输入中出现的日期、数字、单位、区间、比较词和课程名；
3. **来源隔离**：明确区分个人数据、通用知识和课程库，禁止三者互相冒充；
4. **冲突提示**：本地检测同指标多单位、跨日期、缺失值和互相矛盾的描述；
5. **输出合同**：按任务类型选择首段直接回答、表格、列表或短段落，而非一律套模板。

编译器输出只进入单次 Qwen prompt。日志仅保存路由标签、计数、规则 ID 和哈希，不打印现实
用户健康文本。

## 5. 合规创新：单次请求内的生成算法

Qwen 在一次请求内部完成以下隐式过程，但只返回一条最终 Markdown：

```text
读取证据账本
  → 草拟
  → 逐项核对事实/数值/来源/课程
  → 删除无证据内容
  → 检查结构是否服务于当前问题
  → 输出唯一最终回答
```

关键 prompt 策略：

- 事实保守优先：不确定就删除，不猜测个人状态、因果、正常范围或课程名；
- 逐数字溯源：最终回答中的每个个人数字都必须能在个人数据区找到；
- 知识只作解释：通用资料不能改写为该用户已经发生的事实；
- 结构按需：两个以上可比指标或时段才优先用表格，简单问题不强塞表格；
- 核心答案前置：首段直接回应问题，后续只保留证据和可执行建议；
- 唯一输出：禁止候选 A/B、评分、分析过程、JSON 包装和第二份答案。

## 6. 本地后处理边界

允许的确定性处理只包括不会改变语义的操作：

- 去除意外代码围栏或模型分析前缀；
- 统一空行、标题空格和列表缩进；
- 检测截断、空响应、重复段落和边界标签泄漏；
- 生成哈希与去重特征。

本地代码不得补写事实、重算并替换数字、从多个回答中选择，也不得因规则检查失败再次调用
Qwen。规则失败的候选仍按原回答送两次冻结 Judge，保证评分样本没有选择偏差。

## 7. 开发与验收设计

### 7.1 已完成的开发 canary

- 数据：10 个从未用于当前 prompt 调参的新根样本；
- 并发：Qwen 1、Luna 1；
- 硬预算：Qwen 10、Luna 20；
- 每个操作最大尝试：1；
- Judge prompt 哈希必须与冻结版本一致；
- 若累计出现第 2 个非通过事件（Judge 拒绝、Qwen 失败或 Judge 不完整），则 10 条上限最多为
  8/10，立即停止该 prompt；
- 按当前口径至少 8/10 才满足开发点估计不低于 80%，但仍不视为正式证明。

v9 已以 9/10 通过该开发门槛，但未通过随后独立验证。因此 9/10 仅保留为提示候选筛选证据，
不能用于 25K 预算或生产声明。

### 7.2 独立验证

开发 prompt 冻结后，从未参与 prompt 选择的 100 个 validation roots 中预注册 50 根：

- 固定最多 100 次 Qwen；每个 Qwen 成功输出恰好调用两次 Luna，因此 Luna 上限为 200，无重试；
- 观察主指标必须至少 80/100；
- 同时给出精确二项或 Wilson 区间，区分“观察值 >=80%”和“统计下界 >=80%”；
- 验证期间不得根据中途错误调整 prompt；任何调整都开启新的实验 epoch 和新验证集。

v9 validation 在 30 根时因第 10 个非通过事件停止，结果为 20/30。该策略验证失败；未运行的
20 个预注册根与其余 validation/audit roots 继续封存。生产扩容前还需用更大的独立样本确认下
置信界；不能用反复试 prompt 后的同一批数据做认证。

### 7.3 已完成 v10：查询相关性 Evidence Packet

下一轮不再增加自检篇幅，而在 Qwen 之前加入完全本地、无 Judge 标签的证据检索：

1. 对个人数据、专家建议、知识材料与课程库分别做确定性分句；
2. 用当前 query 的字符 n-gram/关键词重叠给原文片段排序，不使用答案、Judge 分数或失败标签；
3. 保留来源隔离的 top-k 原文片段、全部精确课程名和必要的数字/单位邻域，生成带来源 ID 的
   Evidence Packet；
4. Qwen 只基于该证据包执行一次生成，内部仍可审稿，但最终只输出一条 Markdown；
5. 只在新的 development roots 调试 v10；被冻结的 validation/audit roots 不参与任何选择。

实际结果为 0/2 后止损。问题不在检索是否确定性，而在 top-k 包将长材料压缩得过狠，削弱了
回答完整性和跨来源核对能力。v10 不再进入验证。

### 7.4 已完成 v11：完整上下文与相关性索引

v11 保留个人数据、专家材料、知识材料和课程库的全部原文，仅附加不生成事实的确定性相关性
索引。Qwen 在单次请求内执行双草稿核对，但只返回一条最终回答；本地不择优、不重写内容。

开发阶段的已完成评审呈现较强内容信号，但 Luna 端点偶发 502。最终 audit 使用三个健康的
独立 Luna 端点、固定轮转与 90 秒节流，得到 4 Accept / 6 Qwen 请求（66.67%），并满足：

```text
qwen_request_events = 6
luna_request_events = 12 = 2 * 6
failed_request_events = 0
retry_events = 0
strict_protocol_satisfied = true
```

因此 v11 也未达到 >=80%，不能启动 25K 生产。当前 audit 已经用于一次冻结评估，结果不得
反向用于修改并重跑同一 epoch。下一策略必须在新的开发根上设计，重点是无需额外模型调用的
数值关系校验、个人事实溯源约束和“缺证据即删”的生成合同；完成开发后再启用新的未见根认证。

### 7.5 v12/v13 开发结果

v12 加入确定性的个人事实原句卡与数字邻域，要求个人事实“逐字复制或省略”。一次启动阶段
人为中断被如实记为 Qwen 分母且永久禁止重试；运行在 4 根时止损，结果 2/4（4Q/6L）。三个
完成双评审的候选为 2/3 通过，拒绝规则转为 `FACT_LOGIC_ISSUE`、
`CONTRADICT_KB_OR_EXPERT`、`LACK_VISUAL_AID`，未再出现个人数据致命错配。

v13 在事实卡上增加来源优先蓝图：行动建议只取专家材料，知识只作无冲突解释，比较任务按
规则要求表格。N=6 开发臂在 4 根时止损，结果 2/4（4Q/8L）；其中一个完整评审候选得分恰好
14，未满足严格大于 14，另一个候选的两次 Luna 均返回 502。完成双评审的内容侧为 2/3，
聚合规则以 `LACK_VISUAL_AID` 为主，但用户效率口径仍只有 50%，不能剔除教师失败后重算。

为保证可审计恢复，网关现在会记录 `KeyboardInterrupt` 为真实请求事件；同一 operation ID
只要已有成功、失败或中断终态，恢复后都禁止再次发出请求。数据分割也拆分为固定
`split_protected_indices` 与逐 epoch `excluded_indices`，避免新增历史排除根导致 validation/
audit 集合被重新洗牌。

### 7.6 v14：视觉合同与 Luna 容量停批

由于 `summary_train_v3` 的可用开发根已经耗尽，v14 改用与其字段和任务分布最接近的
`summary_train_v36.xlsx`，按文件 SHA-256
`96a6de860ad0a87d7162a17b24cf8a008f93a47f0bacc9ed3976e7f1e3b6d412` 固定本地快照，预先冻结
311/70/100 个 development/validation/audit 根，并排除与 v3 重叠的 28 根。v14 不读取 Judge
标签：它根据原始“画图”要求和任务类型选择紧凑表格或列表，保留逐字个人事实卡，限制建议只
来自专家材料，并禁止表后重复事实。

首个 N=20 开发臂的选择哈希为
`62b316c63a94f2ed1d2135ce9582c7e72af405dc4b93934b964f0104b7aa8f82`。运行在 9 根时止损：

```text
accepted_outputs = 5
qwen_request_events = 9
user_efficiency = 5 / 9 = 55.56%
luna_request_events = 18 = 2 * 9
luna_success_events = 13
luna_failed_502_events = 5
strict_protocol_satisfied = true
```

五个完成双评审的候选全部 Accept，说明 v14 的内容侧信号为 5/5；但用户定义的主指标必须把
Judge 不完整的四个 Qwen 输出留在分母，所以正式结果仍是 5/9，不能宣称达到 80%。该结果
只说明值得在基础设施恢复后继续检验，不能把 5/5 当作主指标或验证结论。

随后保持 v14 内容策略不变，仅把 Luna 调整为单端点、全局 180 秒节流，并在一组全新开发根
上启动 N=10 基础设施 canary；选择哈希为
`2754aef134f50f2c0a6de244af27df623e0a5978c95d6cc2b35d0077f9fd57e2`。第一个 Ground Judge
返回 502，网关保存的安全错误元数据为 `codex_generation_failed`，原因是所选模型处于容量上限。
恢复时没有重试 Qwen 或失败的 Ground Judge，只补齐原定的 Structure Judge；最终账本严格为
1Q+2L，Structure 成功，结果 0/1，随后以 `luna_model_capacity` 停批，没有生成第 2 条 Qwen。

网关现会从持久化事件恢复跨进程节流时钟；v14 在完成当前候选固定的两次 Judge 后，只要任一
Luna 错误明确包含容量不足，就在下一个 Qwen 之前停批。健康检查只能证明本地容器可达，不能
证明指定 Luna 模型有生成容量；在出现模型容量恢复的外部证据前，不应开启新的 Qwen 批次。

容量 canary 后发现 Mac 原生 OpenCode 后端已经退出；两个新根的 Qwen 均如实返回
`opencode_unreachable` 并计入上一 epoch 的 0/2 分母，且不得重试。运行器因此新增只依赖安全
错误码的停批门：当前候选若出现明确 Qwen 后端不可达，必须在下一个 Qwen 前停批。恢复同一
14096 宿主端口后，启动第三个新开发 epoch，选择哈希为
`a46c4b626924b6902ef6eb95ddcfbbf291885b03610d74f7f30e366e59026d2a`，结果为：

```text
accepted_outputs = 9
qwen_request_events = 10
user_efficiency = 9 / 10 = 90.00%
luna_request_events = 20 = 2 * 10
failed_request_events = 0
retry_events = 0
strict_protocol_satisfied = true
```

唯一拒绝同时触发 `FACT_LOGIC_ISSUE` 与 `PERSONAL_DATA_MISMATCH`；分数分布为 8 条 20 分、
1 条 17 分、1 条 0 分。该开发点估计已达到不低于 80% 的目标，但单侧精确 95% 下界仅
60.58%，所以只能选择并冻结 v14 进入独立 validation，不能直接证明生产效率或启动 25K。

v14 随后在选择哈希
`15d3d1bda01757717d7fb46b424f731fc83fd957750531ef4cdcad5c3a94c7cc` 的 20 个独立
validation roots 上评估。用户在运行中明确把目标从“严格大于 80%”修正为“不低于 80%”；
代码因此把 N=20 的最低通过数改为 16，且只有第 5 个非通过才止损。旧进程曾在 12/16 时按
旧边界停下，恢复时前 16 条全部读取缓存，没有重发任何请求。最终第 19 条产生第 5 个非通过，
结果确定为：

```text
accepted_outputs = 14
qwen_request_events = 19
user_efficiency = 14 / 19 = 73.68%
luna_request_events = 38 = 2 * 19
failed_request_events = 0
retry_events = 0
stop_reason = nonaccept_limit_makes_ge_80_impossible
strict_protocol_satisfied = true
```

即使未运行的第 20 条通过，最高也只有 15/20=75%，所以 v14 独立验证失败。该 validation
结果被封存，不得用于修改 v15 prompt 或本地规则，也不得重跑同一批根来洗结果。

### 7.7 v15：证明携带式 Markdown 与本地来源防火墙

v15 只依据 v14 开发阶段已知的事实错配风险和无标签 cheap-verifier 思路设计，不使用已封存
validation 的样本正文或标签。Qwen 仍只调用一次，但每个实质内容行必须携带可移除的来源证明
注释；本地确定性防火墙随后执行：

1. 来源 ID 必须存在于当前输入编译出的精确来源账本；
2. 个人事实行必须逐字包含所引用原文，或通过保守字符重叠校验且不含来源外判断词；
3. 行内所有数字必须存在于该行引用的来源；
4. 课程名必须逐字存在于引用的课程材料；
5. 任一检查失败只删除该行，不补写、不改写、不调用模型；最后去除来源注释。

最终清理后的唯一 Markdown 才送入冻结的两次 Luna Judge。该方法仍严格是每根 1Q+2L；本地
防火墙不读取 Judge 输出，也不能因删除了某行重新调用 Qwen。

v15 共运行五个技术 canary 和一个启用最终词法校验的 N=5 开发臂。早期 canary 暴露了隐藏
注释、非法占位 ID、行内标记解析和过度逐字约束等实现问题；这些请求全部保留在真实账本中。
最终 v15-e 在两个新根上为 0/2，严格账本为 2Q/4L、全部 HTTP 200、attempt=1。第一条只保留
7 个实质行且得 12 分；第二条因 Qwen 漏写证明标记被本地防火墙删除全部 13 个实质行。运行以
`local_firewall_empty` 停止。结论是：让自由文本逐行携带证明的依从性本身不够稳定，v15 不进入
独立验证。

### 7.8 v16：来源规划与确定性 Markdown 编译

v16 不再要求 Qwen 写最终事实文本。一次 Qwen 调用只返回个人、专家、知识和课程来源 ID 的
结构规划；本地编译器随后把被选中的原始证据逐字放入固定 Markdown 结构：

1. Qwen 不输出可进入最终答案的自由文本，只做相关来源选择；
2. ID 不存在就删除；关键来源组漏选或 JSON 无法解析时按查询相关性排序做确定性回退，不重试；
3. 个人记录统一用紧凑表格，专家建议和知识材料用短列表，避免事实改写、漏证明和缺可视化；
4. 没有个人记录时明确材料限制；没有任何可用来源时只请求补充指标、时间、单位和目标；
5. 编译器不读取 Judge prompt、分数、失败标签或 validation 内容。

60 个合成单测以及五个开发根的只读结构审计已经通过；真实 N=5 开发验证使用全新根、预算
5Q/10L，至少 4/5 才能进入下一阶段，第 2 个非通过即数学止损。

真实结果证明纯编译路线不可用：v16 在 2 个新根上 0/2，严格为 2Q/4L；两条都触发
`EMPTY_OR_INCOMPLETE`，分别得到 0 分。逐字来源拼装避免了自由生成幻觉，却系统性损害任务
完成度、自然表达与核心答案前置，因此 v16 止损。

### 7.9 v17/v18：从字段编译回到单次自然成稿

v17 让 Qwen 在 JSON 中同时提供自然表述与来源 ID，本地验证每个字段后编译 Markdown。它在
2 个新根上仍为 0/2（2Q/4L）；第 2 条改善到 Ground 15、Structure 17、总分 12，但仍触发
`FACT_LOGIC_ISSUE` 与 `UNNATURAL_TONE`。这说明本地字段级重写继续破坏回答整体性，v17 止损。

v18 不再本地重写正文，而是组合两个曾有正向信号且合规的机制：v14 的完整上下文、逐字事实卡
和视觉合同，加上 v9 的单次调用内部多稿陪审。Qwen 在一次请求内比较直接回答版、证据稳健版
和自然完整版，再执行事实、冲突、任务、表达、视觉五项否决，最终只输出一份自然 Markdown。
本地只保存哈希和审计元数据，不删改或选择回答。v18 仍严格是 1Q+2L，并只使用全新开发根。

v18 前三根得到 2/3：20 分通过、0 分拒绝、17 分通过。拒绝项只有致命 `ARITH_ERROR`，第三条
仅因 `LACK_VISUAL_AID` 从 20 分扣到 17 分。基于长期已知的数值错配风险而非任何样本正文，
v19 在 v18 上增加通用数字原句屏蔽：禁止所有派生算术、单位换算和跨卡比较；任何含数字的个人
句必须是单张事实卡连续原句。v19 不读取 Judge 内容，也不在本地改写最终正文。

### 7.10 v28：相关性安全正样本路线与冻结审计

v20-v27 继续探索更强合同和可控负样本。结果表明，额外结构破坏虽然能把部分样本推入低分
有效区，也容易越过边界变成 0 分；因此 v28 回到不做本地正文后处理的正样本路线。一次 Qwen
直接输出唯一 Markdown，提示只加入通用的任务相关性、来源不足时的保守回答和安全边界；Judge
prompt、双 Judge 次数和历史本地校验器均未改动。

冻结前的 `summary_train_v36` 开发 canary 为：

```text
scores = [20, 20, 20, 7, 20]
accepted_outputs = 5
qwen_request_events = 5
luna_request_events = 10
efficiency = 5 / 5 = 100%
strict_protocol_satisfied = true
```

随后在 `summary_train_v3.csv` 冻结 audit split 上进行一次 N=5 审计，选择哈希为
`eacc7572f11f4068d546fdfde8acd0453e4dbbdb4957e446f7e681e9769e8895`。独立账本重放结果为：

```text
scores = [17, 17, 0, 17, 20]
positive / negative / ambiguous / zero = 4 / 0 / 0 / 1
accepted_outputs = 4
qwen_request_events = 5
luna_request_events = 10
efficiency = 4 / 5 = 80%
strict_protocol_satisfied = true
complete_scoring_satisfied = true
```

历史本地校验器重放没有改变任何一个分数或分区，且报告不包含源文本。该审计点估计刚好达到
目标，但单侧精确 95% 下界只有 34.26%，不能据此宣称总体通过率已经稳定达到 80%。v28 prompt
在审计后保持冻结；下一步只允许在排除这 5 个已消费根的未见 audit roots 上扩大确认，不能根据
这 5 条的正文或 Judge 标签继续调参。

## 8. 25K 生产停止与预算

生产按冻结批次运行，每个根样本固定 1Q+2L。主口径为：

- 达到 `qwen_request_events == 25,000` 才完成采样预算；
- 同时必须满足 `accepted_outputs >= 20,000` 才达到 80% 目标；
- 连续基础设施失败达到预设阈值时整批停机，不在样本内重试；
- 每批报告实际请求事件、通过数、失败规则和去重损耗；
- 若滚动效率低于 80%，停止开启新批次，回到独立开发集建立新 epoch；
- 旧 epoch 的失败样本不得在同一生产统计中通过重试“洗成”成功。

若交付定义改为 25,000 条已接受数据，则效率刚好为 80% 时理论上至少需要 31,250 次 Qwen
请求和 62,500 次 Luna 请求；实际还要为基础设施失败和去重损耗预留预算。

当前单一共享教师容量按全局 240 秒启动间隔计算：主口径 50,000 次 Luna 请求约需 138.89 天；
若要在 30/14/7 天内完成，分别需要约 5/10/20 条真正独立的等效教师容量通道，不能把同一账号
的多个本地容器误算为独立容量。扩大确认 N=50 需要最多 50Q/100L，当前串行容量约 6.67 小时；
点估计门为至少 40/50，若额外要求单侧精确 95% 下界达到 80%，则需至少 45/50。

生产前已经建立只含索引、哈希和变体号的 25K 冻结调度，选择哈希为
`fb7e03c2e4e90dc3a895a976e0d92fe1c335000e8c29a20376445fe3bf4b7b24`。源文件共有 422 个唯一
根，每根均衡分配 59—60 个变体；25,000 个候选 ID 唯一，分片 ordinal 连续，清单逐条重算根
指纹、源记录指纹、变体 ID 和候选 ID 后通过，且 `contains_source_text=false`。运行器按变体 ID
缓存，避免同一根的后续变体错误复用首个回答；每个分片仍独立核验 1Q+2L、接受率和 exact
dedup。该调度只是预注册元数据，扩大确认门通过且教师产能方案就绪前不得启动 25K 调用。

## 9. 审计不变量

每次运行结束必须验证：

```text
qwen_request_events == attempted_roots
luna_request_events == 2 * successful_qwen_outputs
completed_judgements <= successful_qwen_outputs
accepted_outputs <= completed_judgements
efficiency == accepted_outputs / qwen_request_events
```

若任一等式不成立，运行标记为协议异常，不能用于效率结论。所有报告必须记录数据指纹、模型
名、base URL、prompt 哈希、代码版本、pipeline epoch、请求事件状态和停止原因。
